# Example environment configuration
# Copy this file to `.env` and replace the placeholder values with your secrets.

# Required application settings
DATABASE_URL=postgresql://postgres:password@localhost:5432/accura
SECRET_KEY=change-me-please
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional SendGrid integration (remove both lines if transactional email is disabled)
SENDGRID_API_KEY=SG.change-me
SENDGRID_SENDER=sender@example.com
